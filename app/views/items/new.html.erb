<% provide(:title, '項目登録') %>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(@item) do |f| %>
      <%= f.label :front_text, "表" %>
      <%= f.text_field :front_text, class: 'form-control' %>

      <%= f.label :back_text, "裏" %>
      <%= f.text_field :back_text, class: 'form-control' %>

      <%= f.submit "登録する", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>